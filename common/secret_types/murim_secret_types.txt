########################################
# MURIM CHRONICLES - SECRET TYPES
# Custom secrets for martial world intrigue
########################################

secret_betrayal = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_blood_tempering = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_cheated_trial = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_dark_alliance = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_demonic_cultivation = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_demonic_sect_collaborator = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_demonic_sect_spy = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_double_agent = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_double_dealing = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_faked_assassination = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_forbidden_alchemy = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_forbidden_charm = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_forbidden_garden = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_forbidden_knowledge = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_forbidden_technique = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_heretical_cultivation = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_hidden_disciple = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_lover = {
	category = adultery
	is_valid = { is_alive = yes }
}

secret_mind_control_technique = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_murder = {
	category = murder
	is_valid = { is_alive = yes }
}

secret_murder_plot = {
	category = murder
	is_valid = { is_alive = yes }
}

secret_poisoned_gate_master = {
	category = murder
	is_valid = { is_alive = yes }
}

secret_poisoned_rivals = {
	category = murder
	is_valid = { is_alive = yes }
}

secret_qi_cultivation = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_rigged_election = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_sabotage = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_selling_intelligence = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_shaolin_relic_theft = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_technique_countered_memory = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_technique_theft = {
	category = dishonor
	is_valid = { is_alive = yes }
}

secret_wudang_manipulation = {
	category = dishonor
	is_valid = { is_alive = yes }
}
