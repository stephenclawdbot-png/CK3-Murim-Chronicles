########################################
# MURIM CLAN INTEGRATION EVENTS
# Cross-system integration endpoints for each clan
# AI and player characters both experience these
########################################

namespace = murim_beggar_integration
namespace = murim_demon_integration
namespace = murim_iron_fist_integration
namespace = murim_mount_hua_integration
namespace = murim_phantom_blade_integration
namespace = murim_poison_flower_integration
namespace = murim_shaolin_integration
namespace = murim_tang_integration
namespace = murim_wudang_integration

# ===========================
# BEGGAR SECT INTEGRATION
# Links: information network + jianghu + ranking
# ===========================

murim_beggar_integration.0001 = {
	type = character_event
	title = murim_beggar_integration.0001.t
	desc = murim_beggar_integration.0001.desc
	theme = intrigue
	left_portrait = { character = root animation = personality_compassionate }

	trigger = {
		has_variable = murim_martial_artist
		has_trait = murim_beggar_sect_member
		NOT = { has_character_flag = murim_beggar_integration_done }
	}

	immediate = {
		set_character_flag = { flag = murim_beggar_integration_done years = 5 }
	}

	# Activate the information network across all provinces
	option = {
		name = murim_beggar_integration.0001.a
		add_intrigue = 2
		add_prestige = 100
		change_variable = { name = murim_cultivation_xp add = 30 }
		hidden_effect = {
			every_vassal = {
				limit = { has_trait = murim_beggar_sect_member }
				add_opinion = { target = root modifier = grateful_opinion opinion = 15 }
			}
		}
		ai_chance = {
			base = 50
			modifier = { add = 25 intrigue >= 15 }
		}
	}

	# Focus on personal cultivation instead
	option = {
		name = murim_beggar_integration.0001.b
		add_martial = 1
		change_variable = { name = murim_cultivation_xp add = 50 }
		stress_impact = { ambitious = medium_stress_impact_loss }
		ai_chance = {
			base = 30
			modifier = { add = 20 has_trait = ambitious }
		}
	}

	# Remain humble — the sect endures through patience
	option = {
		name = murim_beggar_integration.0001.c
		add_piety = 150
		add_stress = -20
		ai_chance = { base = 20 }
	}
}

# ===========================
# HEAVENLY DEMON INTEGRATION
# Links: demonic cultivation + shadow politics + calamity
# ===========================

murim_demon_integration.0001 = {
	type = character_event
	title = murim_demon_integration.0001.t
	desc = murim_demon_integration.0001.desc
	theme = skull
	left_portrait = { character = root animation = personality_callous }

	trigger = {
		has_variable = murim_martial_artist
		has_trait = murim_heavenly_demon_member
		NOT = { has_character_flag = murim_demon_integration_done }
	}

	immediate = {
		set_character_flag = { flag = murim_demon_integration_done years = 5 }
	}

	# Seize control of the shadow network
	option = {
		name = murim_demon_integration.0001.a
		add_dread = 30
		add_intrigue = 2
		change_variable = { name = murim_cultivation_xp add = 60 }
		hidden_effect = {
			add_character_modifier = { modifier = murim_demon_lord_aura years = 5 }
		}
		ai_chance = {
			base = 40
			modifier = { add = 30 has_trait = sadistic }
			modifier = { add = 20 intrigue >= 18 }
		}
	}

	# Channel the Heart Demon for a cultivation breakthrough
	option = {
		name = murim_demon_integration.0001.b
		add_prowess = 3
		add_stress = 30
		change_variable = { name = murim_cultivation_xp add = 100 }
		ai_chance = {
			base = 35
			modifier = { add = 25 prowess >= 20 }
		}
	}

	# Withdraw and consolidate power
	option = {
		name = murim_demon_integration.0001.c
		add_prestige = 200
		add_martial = 1
		ai_chance = { base = 25 }
	}
}

# ===========================
# IRON FIST GATE INTEGRATION
# Links: external cultivation + tournament + ranking
# ===========================

murim_iron_fist_integration.0001 = {
	type = character_event
	title = murim_iron_fist_integration.0001.t
	desc = murim_iron_fist_integration.0001.desc
	theme = martial
	left_portrait = { character = root animation = personality_bold }

	trigger = {
		has_variable = murim_martial_artist
		has_trait = murim_iron_fist_disciple
		NOT = { has_character_flag = murim_iron_fist_integration_done }
	}

	immediate = {
		set_character_flag = { flag = murim_iron_fist_integration_done years = 5 }
	}

	# Challenge every ranked martial artist in the region
	option = {
		name = murim_iron_fist_integration.0001.a
		add_prowess = 3
		add_prestige = 200
		change_variable = { name = murim_cultivation_xp add = 40 }
		ai_chance = {
			base = 50
			modifier = { add = 30 prowess >= 18 }
			modifier = { add = 20 has_trait = brave }
		}
	}

	# Focus on iron body tempering
	option = {
		name = murim_iron_fist_integration.0001.b
		add_martial = 2
		change_variable = { name = murim_cultivation_xp add = 60 }
		add_stress = 15
		ai_chance = {
			base = 30
			modifier = { add = 20 has_trait = diligent }
		}
	}

	# Recruit promising fighters to the Gate
	option = {
		name = murim_iron_fist_integration.0001.c
		add_prestige = 100
		add_opinion = { modifier = grateful_opinion target = root modifier_value = 10 }
		ai_chance = { base = 20 }
	}
}

# ===========================
# MOUNT HUA INTEGRATION
# Links: sword arts + clan honor + faction politics
# ===========================

murim_mount_hua_integration.0001 = {
	type = character_event
	title = murim_mount_hua_integration.0001.t
	desc = murim_mount_hua_integration.0001.desc
	theme = martial
	left_portrait = { character = root animation = personality_honorable }

	trigger = {
		has_variable = murim_martial_artist
		has_trait = murim_mount_hua_disciple
		NOT = { has_character_flag = murim_mount_hua_integration_done }
	}

	immediate = {
		set_character_flag = { flag = murim_mount_hua_integration_done years = 5 }
	}

	# Demonstrate the Plum Blossom Sword in a public exhibition
	option = {
		name = murim_mount_hua_integration.0001.a
		add_prestige = 300
		add_martial = 1
		change_variable = { name = murim_cultivation_xp add = 45 }
		ai_chance = {
			base = 45
			modifier = { add = 25 has_trait = ambitious }
		}
	}

	# Seek deeper understanding through meditation on the peak
	option = {
		name = murim_mount_hua_integration.0001.b
		add_learning = 2
		change_variable = { name = murim_cultivation_xp add = 70 }
		add_stress = -25
		ai_chance = {
			base = 35
			modifier = { add = 20 learning >= 14 }
		}
	}

	# Defend the sect's honor against slander
	option = {
		name = murim_mount_hua_integration.0001.c
		add_prowess = 2
		add_dread = 15
		ai_chance = {
			base = 20
			modifier = { add = 20 has_trait = wrathful }
		}
	}
}

# ===========================
# PHANTOM BLADE INTEGRATION
# Links: assassination + shadow arts + espionage
# ===========================

murim_phantom_blade_integration.0001 = {
	type = character_event
	title = murim_phantom_blade_integration.0001.t
	desc = murim_phantom_blade_integration.0001.desc
	theme = intrigue
	left_portrait = { character = root animation = personality_irrational }

	trigger = {
		has_variable = murim_martial_artist
		has_trait = murim_phantom_blade_member
		NOT = { has_character_flag = murim_phantom_integration_done }
	}

	immediate = {
		set_character_flag = { flag = murim_phantom_integration_done years = 5 }
	}

	# Expand the network of hidden blades across the realm
	option = {
		name = murim_phantom_blade_integration.0001.a
		add_intrigue = 3
		add_dread = 20
		change_variable = { name = murim_cultivation_xp add = 50 }
		ai_chance = {
			base = 45
			modifier = { add = 25 intrigue >= 18 }
		}
	}

	# Perfect the Vanishing Step technique
	option = {
		name = murim_phantom_blade_integration.0001.b
		add_prowess = 2
		add_martial = 1
		change_variable = { name = murim_cultivation_xp add = 65 }
		ai_chance = {
			base = 35
			modifier = { add = 20 prowess >= 16 }
		}
	}

	# Lie low — the best blade is the one unseen
	option = {
		name = murim_phantom_blade_integration.0001.c
		add_prestige = 50
		add_stress = -30
		ai_chance = { base = 20 }
	}
}

# ===========================
# POISON FLOWER INTEGRATION
# Links: poison arts + seduction + dual cultivation
# ===========================

murim_poison_flower_integration.0001 = {
	type = character_event
	title = murim_poison_flower_integration.0001.t
	desc = murim_poison_flower_integration.0001.desc
	theme = seduction
	left_portrait = { character = root animation = personality_cynical }

	trigger = {
		has_variable = murim_martial_artist
		has_trait = murim_poison_flower_disciple
		NOT = { has_character_flag = murim_poison_flower_integration_done }
	}

	immediate = {
		set_character_flag = { flag = murim_poison_flower_integration_done years = 5 }
	}

	# Distribute rare toxins through the underground market
	option = {
		name = murim_poison_flower_integration.0001.a
		add_intrigue = 2
		add_gold = 150
		change_variable = { name = murim_cultivation_xp add = 40 }
		ai_chance = {
			base = 40
			modifier = { add = 25 has_trait = greedy }
		}
	}

	# Cultivate the Thousand Petal Body — immunity through poison
	option = {
		name = murim_poison_flower_integration.0001.b
		add_prowess = 2
		change_variable = { name = murim_cultivation_xp add = 80 }
		add_stress = 20
		ai_chance = {
			base = 35
			modifier = { add = 20 prowess >= 14 }
		}
	}

	# Forge alliances through the art of the banquet
	option = {
		name = murim_poison_flower_integration.0001.c
		add_diplomacy = 2
		add_prestige = 100
		ai_chance = {
			base = 25
			modifier = { add = 15 diplomacy >= 14 }
		}
	}
}

# ===========================
# SHAOLIN INTEGRATION
# Links: Buddhist cultivation + martial virtue + tournament
# ===========================

murim_shaolin_integration.0001 = {
	type = character_event
	title = murim_shaolin_integration.0001.t
	desc = murim_shaolin_integration.0001.desc
	theme = faith
	left_portrait = { character = root animation = personality_zealous }

	trigger = {
		has_variable = murim_martial_artist
		has_trait = murim_shaolin_monk
		NOT = { has_character_flag = murim_shaolin_integration_done }
	}

	immediate = {
		set_character_flag = { flag = murim_shaolin_integration_done years = 5 }
	}

	# Open the Shaolin Martial Hall to all sects for joint training
	option = {
		name = murim_shaolin_integration.0001.a
		add_piety = 200
		add_prestige = 150
		change_variable = { name = murim_cultivation_xp add = 35 }
		hidden_effect = {
			every_ruler = {
				limit = { has_variable = murim_martial_artist }
				add_opinion = { target = root modifier = grateful_opinion opinion = 10 }
			}
		}
		ai_chance = {
			base = 45
			modifier = { add = 25 has_trait = compassionate }
		}
	}

	# Attempt the Eighteen Arhat Formation alone
	option = {
		name = murim_shaolin_integration.0001.b
		add_prowess = 3
		add_martial = 1
		change_variable = { name = murim_cultivation_xp add = 75 }
		add_stress = 20
		ai_chance = {
			base = 30
			modifier = { add = 25 prowess >= 20 }
		}
	}

	# Meditate on the Diamond Sutra for inner peace
	option = {
		name = murim_shaolin_integration.0001.c
		add_learning = 2
		add_piety = 100
		add_stress = -30
		change_variable = { name = murim_cultivation_xp add = 50 }
		ai_chance = {
			base = 25
			modifier = { add = 20 learning >= 16 }
		}
	}
}

# ===========================
# TANG CLAN INTEGRATION
# Links: bloodline inheritance + poison mastery + hidden weapons
# ===========================

murim_tang_integration.0001 = {
	type = character_event
	title = murim_tang_integration.0001.t
	desc = murim_tang_integration.0001.desc
	theme = dynasty
	left_portrait = { character = root animation = personality_rational }

	trigger = {
		has_variable = murim_martial_artist
		has_trait = murim_tang_clan_member
		NOT = { has_character_flag = murim_tang_integration_done }
	}

	immediate = {
		set_character_flag = { flag = murim_tang_integration_done years = 5 }
	}

	# Unlock the clan's secret weapon cache
	option = {
		name = murim_tang_integration.0001.a
		add_martial = 2
		add_prowess = 2
		change_variable = { name = murim_cultivation_xp add = 55 }
		ai_chance = {
			base = 40
			modifier = { add = 25 martial >= 16 }
		}
	}

	# Study the ancestral poison compendium
	option = {
		name = murim_tang_integration.0001.b
		add_intrigue = 2
		add_learning = 1
		change_variable = { name = murim_cultivation_xp add = 60 }
		ai_chance = {
			base = 35
			modifier = { add = 20 intrigue >= 14 }
		}
	}

	# Strengthen blood ties — the clan is family above all
	option = {
		name = murim_tang_integration.0001.c
		add_dynasty_prestige = 200
		add_prestige = 100
		ai_chance = { base = 25 }
	}
}

# ===========================
# WUDANG INTEGRATION
# Links: Taoist cultivation + internal alchemy + harmony
# ===========================

murim_wudang_integration.0001 = {
	type = character_event
	title = murim_wudang_integration.0001.t
	desc = murim_wudang_integration.0001.desc
	theme = faith
	left_portrait = { character = root animation = personality_content }

	trigger = {
		has_variable = murim_martial_artist
		has_trait = murim_wudang_disciple
		NOT = { has_character_flag = murim_wudang_integration_done }
	}

	immediate = {
		set_character_flag = { flag = murim_wudang_integration_done years = 5 }
	}

	# Perform the Grand Tai Chi demonstration at the summit
	option = {
		name = murim_wudang_integration.0001.a
		add_prestige = 250
		add_martial = 1
		add_learning = 1
		change_variable = { name = murim_cultivation_xp add = 50 }
		ai_chance = {
			base = 40
			modifier = { add = 20 martial >= 14 }
		}
	}

	# Retreat into internal alchemy — seek the Golden Core
	option = {
		name = murim_wudang_integration.0001.b
		add_learning = 3
		change_variable = { name = murim_cultivation_xp add = 90 }
		add_stress = -20
		ai_chance = {
			base = 35
			modifier = { add = 25 learning >= 16 }
		}
	}

	# Balance the mountain's qi — protect the sacred grounds
	option = {
		name = murim_wudang_integration.0001.c
		add_piety = 200
		add_prestige = 100
		ai_chance = { base = 25 }
	}
}
