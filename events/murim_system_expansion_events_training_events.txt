namespace = murim_training_events


# Training events bridge
murim_training_events.0001 = {
	type = character_event
	title = murim_training_events.0001.t
	desc = murim_training_events.0001.desc
	theme = martial

	trigger = { has_variable = murim_martial_artist }

	option = {
		name = murim_training_events.0001.a
		change_variable = { name = murim_cultivation_xp add = 15 }
		ai_chance = { base = 100 }
	}
}
