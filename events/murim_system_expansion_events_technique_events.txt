namespace = murim_technique_events


# Technique events bridge
murim_technique_events.0001 = {
	type = character_event
	title = murim_technique_events.0001.t
	desc = murim_technique_events.0001.desc
	theme = martial

	trigger = { has_variable = murim_martial_artist }

	option = {
		name = murim_technique_events.0001.a
		change_variable = { name = murim_cultivation_xp add = 20 }
		ai_chance = { base = 100 }
	}
}
