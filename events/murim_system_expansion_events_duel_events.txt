namespace = murim_duel_events


# Duel events bridge
murim_duel_events.0001 = {
	type = character_event
	title = murim_duel_events.0001.t
	desc = murim_duel_events.0001.desc
	theme = martial

	trigger = { has_variable = murim_martial_artist }

	option = {
		name = murim_duel_events.0001.a
		add_prestige = 50
		ai_chance = { base = 100 }
	}
}
